#include <stdio.h>

int generate_structure_specification(const char* structure_type, char* output_buffer, int buffer_size) {
    printf("LLM integration disabled - using file parsing instead\n");
    return 1;  // Return failure so main() falls through to file parsing
}